{% extends 'base.html.twig' %}

{% block title %}BarberProduct{% endblock %}

{% block body %}
    <h1>BarberProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barber_product.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ barber_product.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ barber_product.description }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ barber_product.photo }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ barber_product.prix }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ barber_product.stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('barber_product_index') }}">back to list</a>

    <a href="{{ path('barber_product_edit', {'id': barber_product.id}) }}">edit</a>

    {{ include('barber_product/_delete_form.html.twig') }}
{% endblock %}
